#include "Header.h"

int main()
{
	sf::Vector2f viewSize(1024, 768);
	sf::VideoMode vm(viewSize.x, viewSize.y);
	
	sf::RenderWindow window(vm, "CG_Project By SUMIT & YASHARTH", sf::Style::Default);

	while (window.isOpen()) {

	}
	return 0;
}